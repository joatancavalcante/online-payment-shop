import { styled } from '..'

export const Container = styled('div', {
    display: 'flex',
    width: '100%',
    maxWidth: 'calc(100vw - ((100vw - 1240px) / 2))',
    marginLeft: 'auto',
    minHeight: 656,
    marginBottom: 36,
})